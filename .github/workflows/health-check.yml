name: Podcast Health Check

on:
  schedule:
    - cron: '0 10 * * *'  # Daily at 10:00 UTC (3.5h after the 06:30 podcast generation)
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  actions: write

jobs:
  health-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check latest release
        id: check
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          LATEST=$(gh release list --limit 1 --json tagName,createdAt 2>/dev/null | jq -r '.[0].tagName // empty')
          if [ -z "$LATEST" ]; then
            echo "status=no_releases" >> $GITHUB_OUTPUT
            exit 0
          fi
          CREATED=$(gh release list --limit 1 --json createdAt | jq -r '.[0].createdAt')
          CREATED_TS=$(date -d "$CREATED" +%s)
          NOW_TS=$(date +%s)
          AGE_HOURS=$(( (NOW_TS - CREATED_TS) / 3600 ))
          echo "age_hours=$AGE_HOURS" >> $GITHUB_OUTPUT
          if [ "$AGE_HOURS" -gt 26 ]; then
            echo "status=stale" >> $GITHUB_OUTPUT
          else
            echo "status=healthy" >> $GITHUB_OUTPUT
          fi

      - name: Auto-retrigger podcast if stale
        if: steps.check.outputs.status == 'stale'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Podcast is stale — auto-retriggering Daily Podcast Generation..."
          gh workflow run podcast.yml --ref main -f episode_type=daily
          echo "Workflow triggered. It will generate today's episode."

      - name: Create alert issue if stale
        if: steps.check.outputs.status == 'stale'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --title "Podcast health check: auto-retriggered (last release ${{ steps.check.outputs.age_hours }}h ago)" \
            --body "The daily podcast was expected at 06:30 UTC but the latest GitHub Release is ${{ steps.check.outputs.age_hours }} hours old. The podcast generation workflow has been auto-retriggered. If this issue persists, check the Actions tab for failed workflow runs."

      - name: Report
        run: |
          echo "Status: ${{ steps.check.outputs.status }}"
          echo "Latest release age: ${{ steps.check.outputs.age_hours }}h"
